---
type: llm
focus: files
---
The three pitfalls (the partial unique index on `postings`, the 2026 bank-calendar fixture, and the `ledger-api-v2` consumer group) survive unchanged, because each one is project-specific knowledge the model cannot derive.
