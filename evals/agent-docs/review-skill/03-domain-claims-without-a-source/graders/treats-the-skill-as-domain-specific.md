---
type: llm
focus: last_message
---
Treats the skill as domain-specific and names the version-dependent Terraform claims (`terraform taint`, the claim that Terraform cannot import from configuration, the `~> 4.0` provider pin) as claims that need checking against current Terraform documentation.
