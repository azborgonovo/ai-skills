---
type: llm
focus: last_message
arm: with-only
---
Nothing reaches a live host. Every gh call resolves to the stub in evals/fixtures/bin, every write is recorded in evals/fixtures/writes.log, and the reply is posted against the root comment of the thread through the replies endpoint rather than as a standalone pull request comment.
