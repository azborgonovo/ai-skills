# tools

Each tool sits in its own subfolder, with a README that covers its usage and requirements.

- **[`ralph-loop/`](ralph-loop/README.md)** — Runs a prompt file through the `claude` CLI in a loop. The loop stops when the agent prints a sentinel value, or when it reaches the maximum number of iterations. *Trial*
